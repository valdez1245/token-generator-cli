# Longitud por defecto del token (si no se proporciona por CLI)
DEFAULT_TOKEN_LENGTH=20

# Cantidad de tokens por defecto (si no se proporciona por CLI)
DEFAULT_TOKEN_COUNT=3

# Nombre del archivo por defecto para guardar los tokens
DEFAULT_SAVE_FILENAME=tokens.txt

# Prefijo opcional para los tokens generados
TOKEN_PREFIX=

# Sufijo opcional para los tokens generados
TOKEN_SUFFIX=
